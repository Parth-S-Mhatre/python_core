# tuples are unchange ordered collections of items
info=(21,"male","parth")
print(info.count("parth")) # count the Number of times appears 
print(info.index("male")) # returns the index of the string present in tuple